# Building the VSTS Extension
_(Notes for myself really)_

Install the VSTS tooling:

    yarn global add tfx-cli

Execute the following command from the root:

    tfx extension create --manifest-globs .\vss-extension.json

